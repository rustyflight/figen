[package]
name = "figen"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless = "0.8.0"
figen-proc-macros = {  path = "./proc-macro" }
log = "0.4.27"

[features]
std = ["figen-proc-macros/std"]

[profile.test]
features = ["std"]
